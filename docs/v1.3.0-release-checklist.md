# v1.3.0 发布与逻辑提交清单

> 本清单用于 P1 媒体更新日历发布收口。当前工作树叠加了未提交的 v1.2.0 稳定性改动和 v1.3.0 日历改动；未经用户明确授权，不执行 commit、tag、push 或创建 Release。

## 1. 工作树安全

- [x] 已确认当前分支与 ahead 状态。
- [x] 已确认未提交工作树包含 P0 与 P1，不得 reset、checkout 覆盖或清理未跟踪文件。
- [ ] 拆分提交前保存 `git diff --binary` 补丁备份。
- [ ] 对 P0/P1 交叉文件使用 `git add -p`，逐 hunk 复核。
- [ ] 每个逻辑提交后运行对应测试。
- [ ] 只有获得用户授权后才创建提交、tag 和 push。

如果 P0 仍未提交，先按 [`v1.2.0-release-checklist.md`](v1.2.0-release-checklist.md) 的边界整理 P0，再处理以下 P1 提交。

## 2. P1 建议逻辑提交边界

### Commit A — 日历模型与纯计算服务

建议提交信息：

```text
feat(calendar): derive media update schedule and status
```

范围：

- `src/models/calendar.rs`
- `src/models/mod.rs` 的 calendar 导出
- `src/models/subscription.rs` 的 `manual_schedule` 兼容字段
- `src/services/media_calendar.rs`
- `src/services/mod.rs` 的 calendar 导出
- 相关 Rust 单元测试
- `docs/media-calendar.md`

验收：排期优先级、跨周/跨年、同日多集、未来集数、未知排期和手动覆盖测试通过。

### Commit B — Calendar API 与订阅更新契约

建议提交信息：

```text
feat(api): expose media calendar and manual schedules
```

范围：

- `src/api/calendar.rs`
- `src/api/mod.rs` 的路由注册
- `src/api/subscriptions.rs` 的创建/更新字段存在感知逻辑
- `tests/api_integration.rs` 的日历和手动排期 hunks
- `docs/api-contract.md`

验收：默认自然周、日期范围、状态/媒体类型/订阅筛选、错误参数和 `null` 清除语义通过。

### Commit C — Media Deck 日历页面

建议提交信息：

```text
feat(web): add media update calendar views
```

范围：

- `static/js/features/calendar.js`
- `static/app.js` 的日历状态与 API 装配 hunks
- `static/index.html` 的导航、日历页面和订阅排期编辑器 hunks
- `tailwind/input.css`、`tailwind.config.js` 和生成后的 `static/styles.css` 相关 hunks
- `tests/frontend_calendar.test.js`

验收：周/月/列表、筛选、导航、空/错/未知状态、详情与检查操作，以及 1440px/390px 浏览器回归通过。

### Commit D — v1.3.0 文档、版本和发布准备

建议提交信息：

```text
release: prepare v1.3.0
```

范围：

- `Cargo.toml`
- `Cargo.lock`
- `README.md`
- `CHANGELOG-v1.3.0.md`
- `.github/workflows/release.yml`
- `docs/upgrade-v1.3.0.md`
- `docs/architecture.md`
- `docs/architecture.dot`
- `docs/architecture.svg`
- `docs/architecture.png`
- `docs/roadmap.md`
- 本文件

验收：tag/Cargo 版本、README Release notes、CHANGELOG/升级文档、归档内容和完整质量门一致。

## 3. 版本同步清单

- [x] `Cargo.toml` 为 `1.3.0`。
- [x] `Cargo.lock` 根 package 为 `1.3.0`。
- [x] README 当前镜像标签为 `1.3.0` / `1.3`。
- [x] README 二进制示例使用 `v1.3.0`。
- [x] README 发布命令使用 `v1.3.0`。
- [x] README 存在精确 `### 1.3.0`，供 Release workflow 提取。
- [x] `CHANGELOG-v1.3.0.md` 存在。
- [x] `docs/upgrade-v1.3.0.md` 存在。
- [x] 架构文档和 Graphviz 图标记为 v1.3.0，并包含 Calendar model/service/API、手动排期和前端模块。
- [x] Roadmap 记录实际验证结果与下一执行指针。

## 4. Release 工作流清单

- [x] tag/手动输入版本必须与 Cargo package 版本一致。
- [x] README 必须存在当前版本精确小节。
- [x] `CHANGELOG-v<version>.md` 和 `docs/upgrade-v<version>.md` 必须存在。
- [x] 前端 JavaScript 语法和测试。
- [x] rustfmt、cargo check、clippy `-D warnings` 和完整 Rust 测试。
- [x] Release 构建。
- [x] 归档二进制、`static/`、README、CHANGELOG 和 docs。
- [x] 生成 SHA256。
- [x] 从 README 精确提取 1.3.0 正文且非空。

## 5. 发布级验证清单

- [x] `scripts/build-css.sh`
- [x] 所有 `static/**/*.js` 执行 `node --check`
- [x] `node --test tests/frontend_*.test.js`
- [x] `cargo fmt --all -- --check`
- [x] `cargo check --all-targets --all-features --locked`
- [x] `cargo clippy --all-targets --all-features --locked -- -D warnings`
- [x] `cargo test --all --locked`
- [x] `cargo build --release --locked`
- [x] `git diff --check`
- [x] 本机临时 `DATA_DIR` 启动和健康检查
- [x] 认证 API、Calendar API 与静态资源烟雾测试
- [x] 1440×1000 和 390×844 无头 Chrome 日历回归
- [x] 手动排期保存、覆盖、清除和元数据不变验证
- [x] Release notes 提取、归档内容和 SHA256 校验

## 6. 发布命令（仅获授权后执行）

```bash
git tag v1.3.0
git push origin main
git push origin v1.3.0
```

执行前必须确认：

- 工作树干净；
- HEAD 是预期 v1.3.0 release commit；
- `cargo metadata --no-deps` 显示 1.3.0；
- README 1.3.0 正文能被 workflow 精确提取；
- CHANGELOG、升级文档、架构图和 Release 包内容一致；
- 本文发布级验证项均有实际证据；
- 用户已经明确授权对应 Git/发布操作。
