# v1.13.1 发布检查清单

## 版本与文档

- [x] `Cargo.toml` / `Cargo.lock` 为 `1.13.1`。
- [x] `static/openapi.json` 版本为 `1.13.1`。
- [x] `static/service-worker.js` 使用 v1.13.1 cache key。
- [x] README 存在精确 `### 1.13.1` 小节。
- [x] `CHANGELOG-v1.13.1.md` 与 `docs/upgrade-v1.13.1.md` 存在且非空。

## 质量门

- [x] 前端 JavaScript 语法与 Node 测试通过。
- [x] OpenAPI 路由和兼容性检查通过。
- [x] rustfmt、locked check、Clippy `-D warnings` 和完整 Rust 测试通过。
- [x] Release build、二进制 smoke 和真实浏览器 smoke 通过。
- [x] `git diff --check` 通过。

## GitHub

- [ ] release prep 提交位于 main。
- [ ] annotated tag `v1.13.1` 指向 release prep 提交并已推送。
- [ ] Release workflow 上传 Linux x86_64 归档与 SHA256。
- [ ] GitHub Release 为非 draft、非 prerelease、latest。
- [ ] Docker workflow 发布 `1.13.1`、`1.13` 与 `latest`。
