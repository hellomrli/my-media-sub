# v1.13.0 发布检查清单

## 版本与文档

- [x] `Cargo.toml` / `Cargo.lock` 为 `1.13.0`。
- [x] `static/openapi.json` 版本为 `1.13.0`，路由契约检查通过。
- [x] `static/service-worker.js` 使用新的 v1.13.0 cache key。
- [x] README 存在精确 `### 1.13.0` 小节。
- [x] `CHANGELOG-v1.13.0.md` 与 `docs/upgrade-v1.13.0.md` 存在且非空。
- [x] Telegram 部署、安全、吊销和可选沙箱 smoke 文档完整。

## 质量门

- [x] 前端 JavaScript 语法与 14 个 Node 测试通过。
- [x] `python3 scripts/check-openapi.py` 通过。
- [x] `cargo fmt --all -- --check` 通过。
- [x] `cargo check --all-targets --all-features --locked` 通过。
- [x] `cargo clippy --all-targets --all-features --locked -- -D warnings` 通过。
- [x] `cargo test --all --locked` 通过。
- [x] `cargo build --release --locked` 通过。
- [x] `scripts/smoke-release.sh target/release/my-media-sub` 通过。
- [x] `TELEGRAM_BOT_TOKEN= scripts/smoke-telegram.sh` 安全跳过；真实沙箱仅在 Secret 已配置时执行。
- [x] `git diff --check` 通过，工作树仅含预期 release prep 文件。

## GitHub

- [x] release prep 提交位于 main。
- [x] annotated tag `v1.13.0` 指向 release prep 提交。
- [x] main 与 tag 已推送到 origin。
- [x] Release workflow 成功并上传 Linux x86_64 tar.gz 与 SHA256。
- [x] GitHub Release 为非 draft、非 prerelease、latest。
- [x] Docker tag workflow 成功并发布 `1.13.0`、`1.13` 与 `latest`。
